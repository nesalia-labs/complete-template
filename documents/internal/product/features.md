# DeesseJS — Features Spec

> **The features inventory is now decomposed.** See the [`features/`](./features/) subfolder — one file per surface area, with an index in [`features/README.md`](./features/README.md).
>
> **~325 features across 22 surfaces** (20 original + admin dashboard + SEO, added after flagging gaps), each with status (ship / partial / defer / skip / buyer) and source (competitive / user / differentiator). Meta — status legend, "done" definition, gap analysis, deletions, milestone matrix — is in [`features/README.md`](./features/README.md).
>
> This top-level file is just a pointer. All content lives in `features/`.
