# Local test env. Real Postgres expected at this URL.
# Without a running Postgres, the integration test will fail at connection time.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/deessejs_test
LOG_LEVEL=warn
